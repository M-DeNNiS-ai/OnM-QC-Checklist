const fs=require('fs');const path=require('path');const Database=require('better-sqlite3');const src=path.join(__dirname,'..','data','battery-qc.sqlite');if(!fs.existsSync(src)){console.error('Database does not exist yet. Start the server first.');process.exit(1);}const db=new Database(src);const out=path.join(__dirname,'..','data',`battery-qc-backup-${new Date().toISOString().replace(/[:.]/g,'-')}.sqlite`);db.backup(out).then(()=>{console.log(`Backup created: ${out}`);db.close();}).catch(e=>{console.error(e);db.close();process.exit(1);});
{
  "name": "battery-qc-server",
  "version": "1.0.0",
  "private": true,
  "description": "Battery QC — Express + SQLite backend serving index.html and the /api/auth and /api/data endpoints",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
  "node": "22.x"
},
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cookie-session": "^2.1.0",
    "express": "^4.19.2"
  }
}
